<html>
    <html>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .logedin {
      max-width: 100px;
      margin: 0 auto;
      padding: 20px;
      position: fixed;
       top: 20px;
        right: 20px;
        padding: 10px 20px;
        background-color: #4CAF50;
        color: #fff;
        border: none;
        border-radius: 5px;
        font-size: 16px;
    } body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 20px;
    }

    h1 {
        color: #333;
        font-family: "Arial", sans-serif;
        font-size: 36px;
        color: #333;
        text-align: center;
        text-transform: uppercase;
        letter-spacing: 2px;
        margin-top: 40px;
        margin-bottom: 20px;
      }

    img {
      max-width: 300px;
      margin-bottom: 20px;
    }

    form {
      margin-bottom: 20px;
    }

    label {
      display: block;
      margin-bottom: 5px;
    }

    input[type="text"] {
      width: 100%;
      padding: 8px;
      margin-bottom: 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
      display: inline-block;
      margin-right: 10px;
    }

    input[type="submit"],
    input[type="button"] {
      background-color: #007bff;
      color: #fff;
      padding: 10px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      display: inline-block;
      margin-right: 10px;
    }

    input[type="submit"]:hover,
    input[type="button"]:hover {
      background-color: #0056b3;
      display: inline-block;
      margin-right: 10px;
    }
    .button-container {
  display: flex;
  gap: 10px;
  margin-top: 20px;
}
img.logo {
        position: absolute;
        top: 10px;
        right: 10px;
      }
      .button {
        display: inline-block;
        padding: 10px 20px;
        margin-right: 10px;
        text-decoration: none;
        color: #fff;
        background-color: #333;
        border-radius: 5px;
      }

    </style>
    <div style="display: flex; justify-content: center; flex-wrap: wrap">
      <a href="/movies" class="button">Movies</a>
      <a href="/users" class="button">Manage User</a>
    </div>
    <div class="logedin">
        <% if (dataLogin) { %>
          <div>Welcome, <%= dataLogin.userName %>!</div>
          <!-- Display the username from the dataLogin object -->
        <% } else { %>
          <div>Welcome, <%= dataLogin.userName %>!</div>
          <!-- Display the user's name -->
        <% } %>
        </div>
<img src="https://img.icons8.com/?size=50&id=lEV2xeBg4PUI&format=png" alt="Logo" class="logo">

<body>
    <form action="/users/edit/<%= userdata._id %>" method="POST">
        <h1>Edit User: <%= userdata.userName %></h1> <br/>
      
        <label for="userName">Name:</label>
        <input value="<%= userdata.userName %>" name="userName" type="text" /><br/>
      
        <label for="password">Password:</label>
        <input value="<%= userdata.password %>" name="password" type="text" /><br/>
      
        <label for="permession">Permission:</label>
        <input value="<%= userdata.permession %>" name="permession" type="text" /><br/>
      
        <label for="_id">Mongoose Id:</label>
        <input value="<%= userdata._id %>" name="_id" type="text" /><br/>
      
        <div class="button-container">
          <input type="submit" value="Update" />
          <form action="/users/all">
            <input type="submit" value="Cancel"  style="background-color: red;" />
          </form>
        </div>
      </form>
</body>
</html>